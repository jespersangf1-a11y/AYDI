# AYDI — Verbesserungsprotokoll

**Auftrag:** Gesamtverantwortung für Qualität und Einsatzfähigkeit (Bereiche A–D).
**Begonnen:** 22.08.2026
**Branch:** audit/fixes-checkpoint

## Ausgangslage (verifiziert, nicht übernommen)

| Fakt | Wert | Erhebung |
|---|---|---|
| Testsuite | 1224 passed, 0 failed (24,6 s) | `PYTHONPATH=. pytest tests/ -q` |
| Backend-Import | sauber, 126 Routen | `from app.main import app` |
| Wissenskorpus | 261 nummerierte Docs, 32 Kategorien, 851.217 Zeilen | Dateisystem |
| CLAUDE.md behauptet | 252 Docs, 31 Kategorien | CLAUDE.md → **Drift** |
| Analysemodule | 11 | `app/services/analysis/` |
| Korpus-Längenverteilung | 9 Docs <400 Z. (alle Kat. 32), 4 × 400–800, 33 × 800–1500, 10 × 1500–3000, 205 × >3000 | Dateisystem |

## Legende

Bereich: **A** Recherche-Wissensbasis · **B** Code & Backend · **C** Spezifikation · **D** Systemintegration
Status: **erledigt** · **empfohlen** (Entscheidung mit Tragweite, Optionen dokumentiert) · **zurückgestellt**

---

## Änderungen

| # | Bereich | Fundstelle | Problem | Maßnahme | Prio | Status |
|---|---------|-----------|---------|----------|------|--------|
| 1 | D/B | `backend/praxistest_full.py:11`, `praxistest_runner.py:13` | End-to-End-Praxistest war **nicht lauffähig**: DB- und Ergebnispfad zeigten auf ein längst verschwundenes Sandbox-Verzeichnis (`/sessions/dreamy-youthful-fermi/`). Der Test, der die Einsatzfähigkeit belegen soll, konnte seit dem Umzug nie mehr laufen. | Pfad auf ein Verzeichnis neben dem Skript umgestellt, per `PRAXISTEST_OUT_DIR` überschreibbar. | hoch | erledigt |
| 2 | B | `backend/praxistest_full.py`, `praxistest_runner.py` (Ausgabe) | Auf Windows — der dokumentierten Hauptentwicklungsplattform — brach der Praxistest sofort mit `UnicodeEncodeError` ab (cp1252 kann die Status-Emojis nicht kodieren). | stdout/stderr werden auf UTF-8 umgestellt (defensiv, mit Fallback). | hoch | erledigt |
| 3 | B | `backend/praxistest_full.py` A7 | Testdefekt: Der Auth-Guard wurde mit dem Client geprüft, der aus vorherigen Logins noch Session-Cookies hielt — es wurde also ein *authentifizierter* Zugriff gemessen. Ergebnis: 7 Endpunkte fälschlich als „ungeschützt“ gemeldet. | Guard-Prüfung nutzt jetzt einen frischen, cookiefreien `TestClient`. | hoch | erledigt |
| 4 | B/C | `backend/praxistest_full.py` A7 | Testdefekt: `/api/v1/knowledge/categories` wurde als „muss 401 liefern“ geprüft, obwohl Produktsäule 1 diesen Endpunkt bewusst öffentlich anbietet (CLAUDE.md: „Public read“). Der Test hätte die korrekte Produktentscheidung als Fehler markiert. | Aufgeteilt in `A7-AuthGuard` (6 geschützte) und neu `A7b-PublicRead` (öffentlich erreichbar). | mittel | erledigt |
| 5 | B | `backend/praxistest_full.py` A10/B2 | Testdefekt: Der Praxistest simuliert einen reinen Bearer-Client, behielt aber die Session-Cookies aus `/auth/login`. Da `_extract_token` das Cookie dem Bearer-Header **vorzieht**, war jeder Folge-Request cookie-authentifiziert und der CSRF-Schutz griff zu Recht → 4× 403, Folgefehler in Gruppe B. Der CSRF-Middleware-Code ist korrekt; das Frontend (`api.ts`) echot den Token richtig. | Client leert die Cookie-Jar nach jedem Request → echter Bearer-Client. Praxistest jetzt **116 PASS / 1 FAIL** statt 107/6. | hoch | erledigt |
| 6 | A/D | `backend/app/services/knowledge/markdown_knowledge_loader.py` | **Größter Einzelfund Bereich A.** Der Korpus ist in mehreren Dokumentkonventionen gewachsen (Kat. 01: `**Firmenprofil:**`-Blöcke; andere: `## N Hersteller-Datenbank`, `**Hersteller:** X` inline, Feld/Wert-Praxisbericht-Tabellen). Die Extraktoren kannten nur die Konventionen der Kategorie 01. Messung vorher: **22/260** Dokumente lieferten Herstellerdaten, **8/260** Erfahrungsberichte. Das Wissen stand im Text, war für Analyse und Lexikon aber unsichtbar — z.B. `03_01_antifouling` (3.842 Zeilen, echte Teilenummern, 10 belegte Forenberichte) lieferte 0/0. | Konzeptbasierte Abschnittssuche (`_iter_concept_sections`) + formatunabhängige Auslese (Fettmarke, Tabelle, Unterüberschrift, Blockzitat, Feld/Wert-Tabelle). Metazeilen (`> Quelle:`, `> Confidence:`) werden als Herkunft statt als Berichtstext gewertet. Ergebnis: **163/260** mit Herstellern (2.422 Einträge), **61/260** mit Erfahrungsberichten (1.709 Einträge), inkl. Modellzuordnung, Revier und Jahr. | hoch | erledigt |
| 7 | A | `markdown_knowledge_loader.py:1318` | Hersteller aus `**Firmenprofil:**`-Blöcken wurden ohne `name`-Feld gespeichert, wenn der Name aus `origin` abgeleitet wurde → namenlose Einträge in Lexikon und API. Von einem neuen Test aufgedeckt. | `profile["name"]` wird gesetzt, bevor der Eintrag übernommen wird. | mittel | erledigt |
| 8 | A/B | `backend/tests/test_markdown_knowledge_loader.py` | Die Extraktionsabdeckung war durch nichts abgesichert (Tests prüften nur lose Untergrenzen wie „>= 10 Hersteller"), ein stiller Rückfall wäre unbemerkt geblieben. | 11 neue Tests: Korpusabdeckung Hersteller/Erfahrungen, keine Metazeilen als Berichtstext, plausible Namen, Quellenpflicht, Unit-Tests der Abschnittssuche. | mittel | erledigt |
| 9 | A | Korpus, 6 Dokumentpaare | **Taxonomie-Dopplung:** 12 Dokumente behandeln dasselbe Thema in zwei Kategorien (13_02/17_02 Ankerketten, 13_03/17_03 Ankerwinden, 13_04/17_04 Ankergeschirr, 14_03/20_02 Hydraulische Steuerung, 14_04/20_03 Ruderanlage/Lager, 14_07/20_04 Steuerräder/Pinnen). Es sind **unabhängig geschriebene** Texte (nur ~2 % gemeinsame Zeilen, je ~3.800 Zeilen), die zwangsläufig auseinanderdriften. | Analysiert und belegt; Zusammenführung ist eine inhaltliche Entscheidung mit Tragweite → siehe Empfehlungen. | mittel | empfohlen |
| 10 | C | `KNOWLEDGE_INDEX.py:958` vs `:965`, `CLAUDE.md` | Drei widersprüchliche Korpusgrößen: CLAUDE.md „252 Dokumente / 31 Kategorien", KNOWLEDGE_INDEX Zeile 958 „252 / 31", Zeile 965 „261 / 32". Tatsächlich: **260 kanonische Dokumente in 32 Kategorien** (261 nummerierte Dateien minus `24_05_pumpen_sanitaer_clean.md`, eine ältere Zwischenkopie, die der Loader korrekt überspringt). | Zahlen werden in CLAUDE.md und KNOWLEDGE_INDEX auf 260/32 vereinheitlicht (siehe Bereich C). | mittel | offen |
| 11 | B | `app/core/validation.py`, `app/core/domains.py`, `orchestrator.py:199` | **Zonen verloren still ihre Domänenzuordnung.** (a) `galley` — der englische Standardbegriff für die Bordküche — war kein gültiger Zonentyp (kanonisch: `pantry`); unbekannte Typen wurden nur geloggt und unverändert weitergereicht, `get_domain_for_zone_type()` lieferte `None`, die Zone fiel aus der Domänen-Abdeckung. Das Ergebnis wirkte vollständig, war es aber nicht. (b) Die zwei Vokabulare waren um je 4 Einträge auseinandergelaufen (`charger_area`, `boatyard`, `maintenance_hatch`, `service_area` nur in den Domänen; `crew_area`, `guest_area`, `technical`, `void` nur in der Validierung, ohne Domäne). (c) Der Rückgabewert von `validate_zones()` wurde im Orchestrator **verworfen** — die Validierung konnte gar nicht normalisieren. | 40 Synonyme (DE+EN) → kanonischer Typ, Rückschreibung in die Zone, beide Vokabulare deckungsgleich, Orchestrator übernimmt das Ergebnis. 19 neue Tests. | hoch | erledigt |
| 12 | B | 11 Analysemodule (`ergonomics.py`, `materials.py`, …) | **Interner Fehler wurde zur schlechten Note.** Brach eine Teilanalyse mit einer Exception ab, ging sie mit **0.0** in den gewichteten Mittelwert ein. Beispiel gemessen: Ergonomie fiel von 57,8 auf 47,6, obwohl über das Boot nichts Negatives bekannt war. Das widerspricht der Grundregel „lieber nicht beurteilbar als geraten" und präsentiert dem Nutzer ein erfundenes Messergebnis. | Neuer Helfer `analysis/subscore.py`: fehlgeschlagene Teilanalysen fliegen aus Zähler **und** Nenner, Gewichte werden neu normiert. Ausfälle stehen als `degraded_subanalyses` in der Antwort; fällt alles aus, meldet das Modul `available: false` (Modul-Skip-Vertrag). 9 neue Tests. | hoch | erledigt |
| 13 | B | `app/services/analysis/materials.py` (9 Stellen) | `zm["material"]` warf `KeyError`, wenn eine Zonen-Material-Zuweisung keinen Materialblock hatte — riss zwei Teilanalysen ab. | Auf `zm.get("material") or {}` umgestellt. | mittel | erledigt |
| 14 | B | `app/core/middleware.py` | **Die App setzte keinen einzigen Sicherheitsheader.** Kein `X-Content-Type-Options`, kein `X-Frame-Options`, keine CSP, kein `Referrer-Policy`. Nutzertext wird in JSON-Antworten zurückgespiegelt; ohne `nosniff` darf ein Browser so eine Antwort als HTML deuten. | `SecurityHeadersMiddleware` (nosniff, DENY, no-referrer, CSP `default-src 'none'`, CORP same-site; HSTS nur bei `COOKIE_SECURE`). Wirkt auch auf Fehlerantworten. 10 neue Tests. | hoch | erledigt |
| 15 | D/B | `backend/praxistest_full.py` B3 | Der Praxistest behauptete, die Vollanalyse zu prüfen, erwartete aber ≥10 Module für eine **FREE**-Nutzerin, die per Tarif nur vier bekommt → seit Einführung des Tarif-Gatings dauerhaft rot, **der Profi-Pfad war faktisch ungeprüft**. | B3 prüft jetzt korrektes FREE-Gating; neu **B3b** hebt Sarah (Werftleiterin → laut Säulen-Tabelle PRO) auf PRO und verlangt Rechenschaft: jedes freigeschaltete Modul läuft **oder** nennt einen Klartextgrund; nichts darf unerklärt fehlen. | hoch | erledigt |
| 16 | D | `backend/tests/test_e2e_full_analysis.py` (neu) | Der Flaggschiff-Pfad (Profi-Projekt mit Materialien, Kosten, Serviceberichten, Wettbewerbern, Marken-Referenzen durch **alle** Module) war nirgends abgesichert. | 12 End-to-End-Tests: alle 12 Module laufen mit vollständigen Daten; ohne Daten wird mit Klartextgrund übersprungen statt geraten; Tarif-Gating getrennt ausgewiesen; Synonym `galley` behält seine Domäne. | hoch | erledigt |
| 17 | B | `backend/praxistest_full.py` K1/E4 | Zwei Prüfungen gaben **falsche Entwarnung**: K1 verglich die Domänen nur mit sich selbst (trivial erfüllt, während die Vokabulare auseinanderliefen); E4 warnte über in JSON gespiegelte Script-Tags, was für eine JSON-API kein XSS ist. | K1b (Vokabular-Deckung) und K1c (Synonyme) ergänzt; E4 prüft jetzt den echten Schutz (`nosniff` + `application/json`). Praxistest: **123/123, 0 Warnungen**. | mittel | erledigt |
| 18 | B | `.env.local` (getrackt, Commit `d5162c5`) | **KRITISCH: Produktiver Anthropic-API-Key und DB-Zugangsdaten lagen versioniert im Repo.** `.gitignore` enthielt zwar `.env.*`, die Regel greift aber nicht für bereits getrackte Dateien. | `git rm --cached .env.local` — Datei lokal erhalten, ab jetzt ignoriert. **Der Schlüssel bleibt in der Git-History; er MUSS rotiert werden** (siehe Sofortmaßnahmen). | kritisch | erledigt + Handlungsbedarf |
| 19 | B/D | `render.yaml` | Der Produktions-Sicherheitsguard in `config.py` (`_enforce_production_security`) war **in jedem mitgelieferten Deployment-Pfad wirkungslos**: Kein Manifest setzte `ENVIRONMENT=production`. Ein vergessener `SECRET_KEY` hätte still zum repo-öffentlichen Default geführt — mit dem sich JWTs inklusive Admin-Rolle fälschen lassen. | `ENVIRONMENT=production` in `render.yaml` gesetzt (fail closed) und begründet. | kritisch | erledigt |
| 20 | A | `29_04_signalmittel.md` §2.1–2.3 | **Sicherheitskritisch erfundene Inhalte.** In der SART-Tabelle standen `Mammarth SART4` (Hersteller existiert nicht), `Furuno FAR-2127` (ist eine **Radaranlage**) und `ACR AquaLink SART` (AquaLink ist eine Baken-Reihe). In der EPIRB-Tabelle standen PLBs (`ResQLink`, `FastFind`) und eine GPS-Baureihe (`JRC JLR-7250`). Wer danach beschafft hätte, hätte **kein funktionsfähiges SART an Bord**. Das Dokument wird über die Knowledge-API unverändert ausgeliefert. | Abschnitt neu geschrieben und **web-verifiziert**: getrennte Tabellen EPIRB / PLB / SART mit belegten Produkten (Jotron Tron SART20, McMurdo S4, SAILOR SART II, ACR Pathfinder PRO 2914, Ocean Signal S100; EPIRBs ACR GlobalFix, McMurdo SmartFind, Ocean Signal SafeSea, Jotron Tron 40S/60S). Warnkasten zur Klassenverwechslung, 7 Quellen mit Abrufdatum, entfernte Falscheinträge als ⚠️-Audit-Spur dokumentiert. | kritisch | erledigt |
| 21 | A | `05_07:2846`, `05_07:875`, `31_08:46`, `31_09:1052`, `knowledge_retrieval.py:1124` | ISO-Fehlzitate, vor denen CLAUDE.md ausdrücklich warnt: **ISO 12217 ist die Stabilitätsnorm**, wurde aber für Chainplate-Dimensionierung, Tankwandstärke und Kielkonstruktion zitiert; Ruder-Beispiel trug `ISO 12217-3`; im Code stand `ISO 13849` (Maschinensicherheit) als Bordnetz-Norm. | Korrigiert auf ISO 12215 / 12215-8 / 12215-9 / 21487 bzw. ISO 13297 + 10133, jeweils mit ⚠️-Audit-Spur und Begründung. | hoch | erledigt |
| 22 | A/C | `KNOWLEDGE_INDEX.py:958` | Die Datei widersprach sich sieben Zeilen weit selbst (Kommentar „252/31", Beschreibung „261/32") und nannte ~842.000 Zeilen. | Auf gemessene Werte vereinheitlicht: **261 nummerierte Dateien in 32 Kategorien (850.268 Zeilen), davon 260 kanonisch geladen (847.782 Zeilen)**. | mittel | erledigt |
| 23 | B/D | `dxf/parser.py:40`, `ergonomics.py`, `schemas.py` | **KRITISCH: Der DXF-Import erfand Durchgangsbreiten.** `width_mm = max(tolerance, min_dist)` mit `tolerance=100` — `min_dist` ist aber der Abstand der nächsten *Eckpunkte*, bei angrenzenden Zonen also ~0. Jeder importierte Durchgang bekam exakt 100 mm, und die Ergonomie meldete ihn als „kritisch schmal" **mit Konfidenz `measured`**. | Breite wird als `None` / `width_source: "unknown"` geführt; Ergonomie behandelt sie als nicht beurteilbar (eigene Info-Warnung, kein Score-Effekt); Response-Schema entsprechend optional. Zusätzlich Bounding-Box-Vorfilter gegen die O(n²·m²)-Laufzeit und Schutz gegen nicht-finite/riesige Koordinaten (vorher `OverflowError` → 500). 20 neue Tests. | kritisch | erledigt |
| 24 | B/D | `service_patterns.py` | **Ein gemeldeter Schaden verbesserte die Bewertung.** Gemessen: Schwere hatte **gar keinen** Einfluss — 10 kosmetische und 10 kritische Befunde ergaben identisch 90,0. Zusätzlich war die Note nicht monoton: ab 31 Berichten schaltete ein relativer Filter alle Befunde ab, 60 Totalschäden (93,8) wurden besser bewertet als 20 (90,0). Ursache: Der Filter verlangte, dass eine Zone sich vom Zonen-Mittelwert abhebt — bei nur einer betroffenen Zone ist der Mittelwert genau deren Wert. | (a) Relativer Filter greift nur noch ab 3 Zonentypen. (b) Neue Teilanalyse `severity_burden` (Gewicht 0,26, übrige auf 0,74 reskaliert, Summe bleibt exakt 1,0): gewichtete Schadenslast, kalibriert so, dass ein kritischer Befund bei 66,7 landet. Ergebnis: Schwere wirkt (low 91,7 / critical 66,7), Note ist über alle Mengen monoton. 14 neue Tests, gegengeprüft rot ohne Fix. | kritisch | erledigt |
| 25 | B | 11 Analysemodule + `production.py` | Folgefehler der ehrlichen „unbekannten Breite": `_dominant_share` warf `max() on empty sequence`, wenn keine Breite bekannt war. Vom End-to-End-Lauf gefunden, nicht von den Unit-Tests. | Leere Eingabe wird als „nichts zu vergleichen" behandelt. | mittel | erledigt |
| 26 | B | 8 Dateigruppen (Parallel-Batch) | Bestätigte Audit-Befunde: NaN/Infinity auf allen Eingabeebenen (ROB-1/2), unbegrenztes `cabin_count` (ROB-3), unbegrenzte Listen (SEC-9), STEP/IGES ohne Größenlimit (ROB-5/SEC-8), Vision-Konfidenzwächter wirkungslos (B-1/2/5/13), kein Schema-Guard nach JSON-Parsing (B-10/11), wirkungsloses Timeout (B-9), verworfene Befunde (B-14), ungeskalierte Bilder (B-3), Prompt-Injection über `zone_type` (SEC-3/B-6), Bootsklassen-Fallback (B-3), **Pipeline B lief auf der Upload-Route nie** (`asyncio.run` im laufenden Loop, Fehler verschluckt — ROB-7/D-1), fehlendes Tarif-Gate für Vision/CAD/Report/Versionen (SEC-4/D-9), i18n-Zahlformat und 5 fehlende Bootsklassen (I18N-7/3). | In 8 disjunkten Dateigruppen parallel behoben, je mit Vorher/Nachher-Reproduktion und Regressionstest (jeder Test gegengeprüft rot ohne Fix). Suite von 1284 auf **1625 Tests** gewachsen. | hoch | erledigt |
| 27 | C | `CLAUDE.md` | 16 Spezifikationsabweichungen: Korpusgröße (3 Stellen), Score-Fusion-Abschnitt beschreibt nicht verdrahteten Code, `community`-Modul fehlte komplett (12 statt 11 Module), falsche Cockpit-Drain-Formel, falscher Modulname `volume`, nicht existierende Profi-Features, falsche Schriftarten, SciPy, Heel-Winkel, „all 9 route files". Außerdem: CLAUDE.md allein machte einen neuen Durchlauf nicht arbeitsfähig (keine Migrationen, kein Env-Setup, kein `npm install`). | Alle 16 korrigiert; neuer verifizierter **„Getting Started"-Abschnitt** (jeder Befehl real ausgeführt); Score-Fusion als **NICHT VERDRAHTET** gekennzeichnet; die neuen Mechanismen (`subscore.py`, Zonentyp-Aliase, Security-Header, `ENVIRONMENT=production`) dokumentiert. 21 neue Tests halten Spezifikation und Code deckungsgleich. | hoch | erledigt |

| 28 | D | 7 Frontend-Dateigruppen | **Der Backend-Pfad war für echte Nutzer zur Hälfte unerreichbar.** Verifiziert: `getClassBenchmarks` rief `/benchmarks/…` auf, während das Backend `/class-benchmarks/…` bedient (garantiertes 404); `createProject` postete auf `/projects/` mit Schrägstrich (307-Redirect); der beim Login gelieferte `refresh_token` wurde **nie benutzt** (harter Abbruch nach 60 min); `tier_gated`, `skipped`, `errors` und `validation_warnings` kamen im gesamten Frontend **nicht ein einziges Mal** vor — für FREE-Nutzer verschwanden 8 von 12 Modulen kommentarlos; die Bild-Upload-Komponente akzeptierte HEIC, das das Backend mit 400 ablehnt (**iPhone-Standardformat**); Serviceberichte und Kostenpositionen ließen sich nirgends anlegen, wodurch `service_patterns` und `cost` dauerhaft „nicht auswertbar" meldeten. | In 7 disjunkten Dateigruppen parallel behoben, jede gegen die **echte** Backend-Route und das echte Pydantic-Schema geprüft. Neu: Refresh-Flow mit Einmal-Wiederholung und Schutz gegen parallele Doppel-Refreshes; vier unterscheidbare Modul-Zustände (tarifgesperrt / mangels Daten übersprungen / fehlgeschlagen / teilweise berechnet inkl. `degraded_subanalyses`); Erfassungsformulare für Serviceberichte und Kosten in eigenen API-Modulen (`service-api.ts`, `cost-api.ts` — `api.ts` blieb in einer Hand). `npm run build` (tsc strict + vite) grün. | hoch | erledigt |
| 29 | A/B | `markdown_knowledge_loader.py::_extract_fehlerbilder` | **Die handlungsrelevanteste Information des Korpus ging verloren.** Von 985 Fehlerbildern hatten nur **322 eine Maßnahme**, 428 bestanden nur aus einem Titel. Ursache wieder die Konventionsvielfalt: Der Extraktor kannte `**Maßnahme:**`, der Korpus schreibt auch `Lösung`, `Behebung`, `Abhilfe` und `Massnahme` (ohne Eszett); die häufigste Form überhaupt — Katalog-Tabellen `\| Nr \| Fehlerbild \| Symptom \| Ursache \| Lösung \|` — wurde gar nicht ausgelesen. Folgewirkung: Analysewarnungen aus dem Wissenskorpus trugen keinen Handlungsvorschlag. | Eine gemeinsame Label-Definition (`_FEHLERBILD_FIELD_LABELS`) für alle drei Fundstellen (Fettmarke, Merkmal/Details-Tabelle, Katalog-Tabelle). Ergebnis: **Maßnahme 322→516**, Symptom 366→641, Ursache 494→622, „nur Titel" 428→229. | hoch | erledigt |
| 30 | B | `materials.py`, `service_patterns.py`, `structural.py` | Konventionsbruch gemessen: **17 von 90 Warnungen ohne Handlungsvorschlag**, obwohl CLAUDE.md „Every warning has a suggestion" festlegt. | Durch Befund 29 auf 11 gesenkt; für die restlichen (Korpus führt dort wirklich keine Maßnahme) verweist der Vorschlag jetzt auf den Quellartikel, statt einen Rat zu **erfinden**. Jetzt **0 von 90**. Neuer Test `test_warning_conventions.py` erzwingt beides dauerhaft — inklusive Schutz gegen einen leeren Testlauf und gegen inhaltsleere Platzhalter-Vorschläge. | mittel | erledigt |

| 31 | B/D | `app/core/validation.py` | **Fünf der dreizehn offiziellen Bootsklassen wurden abgelehnt.** `VALID_BOAT_CLASSES` war eine handgepflegte Kopie und gegenüber dem `BoatClass`-Enum verrutscht: `racing_sail`, `daysailer`, `catamaran_motor`, `sport_cruiser`, `explorer` fielen durch die Validierung, obwohl das Frontend sie zur Auswahl anbietet und alle Analysemodule Defaults für sie führen; fünf längst zurückgezogene Namen galten weiter. Besonders betroffen: die öffentliche Schnellanalyse nimmt `boat_class` als **freien String** (nicht als Enum), die Prüfung passiert also erst hier — wer „Daysailer" wählte, lief in eine Validierungswarnung. | Liste wird aus dem Enum **abgeleitet** statt dupliziert (Drift dauerhaft ausgeschlossen); `BOAT_CLASS_ALIASES` zieht die fünf Altnamen auf ihren kanonischen Wert, Näherungen dabei ausdrücklich als solche gekennzeichnet. 32 neue Tests; ein Test, der den Altnamen als kanonisch festschrieb, wurde mit Begründung korrigiert. | hoch | erledigt |
| 32 | B | `.env.example`, `README.md` (neu) | **Kaltstart war nicht dokumentiert.** `.env.example` deckte 5 von 18 Settings ab — alle sicherheitsrelevanten fehlten (`SECRET_KEY`, `COOKIE_SECURE`, `ENVIRONMENT`, `TRUST_PROXY_HEADERS`). Ein README existierte im ganzen Repo nicht. | `.env.example` dokumentiert jetzt **alle 18** Settings mit Defaults, Wirkung und Produktions-Checkliste (programmatisch gegen `Settings.model_fields` abgeglichen: 0 fehlend, 0 erfunden). Neues README mit verifiziertem Schnellstart — jeder Befehl, jede Zahl (19 Routenmodule, 12 Analysemodule, 7 Migrationen, 260 Dokumente/847.782 Zeilen) vor dem Schreiben ausgeführt bzw. gemessen. | mittel | erledigt |
| 33 | A/D | `markdown_knowledge_loader.py::search_markdown_knowledge` | **Die Lexikon-Suche — Kernbedienung von Produktsäule 1 — fand das Dokument selbst nie.** Gesucht wurde nur in FAQ, Glossar, Fehlerbildern und Erfahrungsberichten, nie im Titel und nie in Tabellen (dort stehen Teilenummern, Maße, Preise). Der Docstring behauptete trotzdem „Full-text search". Dazu **keinerlei Sortierung**: Treffer kamen in Korpus-Reihenfolge und wurden willkürlich abgeschnitten — bei „Osmose" stand das Osmoseschutz-Dokument nicht unter den ersten fünf. | Dokumenttreffer (Titel + Slug), Tabellensuche und Relevanzsortierung (Titel > Fehlerbild > FAQ > Glossar > Erfahrung > Tabelle, Bonus für Wortgrenze und Präfix). Suchbegriff wird für das Wortgrenzen-Muster escaped. Dazu ein einmalig gebauter Suchindex: **400 ms → 8 ms (Fehltreffer) bzw. 78–170 ms (häufige Begriffe)**, Aufbau 0,5 s, 31 MB. 18 neue Tests inkl. Sortierstabilität und Regex-Sonderzeichen. | hoch | erledigt |

| 34 | B/D | `routes/quick_analysis.py`, `services/reports/pdf_generator.py` | Beide führten eine eigene Tabelle mit **4 der 13** Bootsklassen; alle übrigen fielen per `.get(bc, bc)` auf den **rohen englischen Enum-Schlüssel** zurück. Im deutschen Ergebnistext der Schnellanalyse und im PDF-Report stand dann „daysailer" statt „Daysailer / Tagessegler". | Beide an den i18n-Katalog angebunden, der inzwischen alle 13 Klassen in DE/EN/ES/FR führt. Gegenprobe: 0 Klassen ohne Übersetzung. | mittel | erledigt |
| 35 | B | `app/core/middleware.py` (Rate-Limit) | **Die teuersten Routen waren am schwächsten gedrosselt.** Die Zuordnung lief über reine Präfixe: `/api/v1/projects/{id}/images` — die Route, die eine **kostenpflichtige** Claude-Vision-Analyse auslöst — beginnt mit `/api/v1/projects` und landete im 120/min-Eimer statt im 10/min-Eimer. Der konfigurierte Eimer `/api/v1/import-cad` existierte als Route **gar nicht**; die echten CAD-Pfade lagen ebenfalls bei 120/min — er war vollständig wirkungslos. | Gemusterte Zuordnung statt Präfix, Reihenfolge nach Kosten (teuerste zuerst), sodass eine Unterroute nie in den großzügigeren Eimer ihres Elternpfads fällt. Zähler zurücksetzbar, damit Autorisierungstests nicht am Limit statt am Prüfgegenstand messen. 21 neue Tests. | hoch | erledigt |
| 36 | B | `app/core/middleware.py` (CSRF) | Die Ausnahme `/api/v1/quick-analysis` galt als **Präfix** und nahm damit auch die authentifizierte Unterroute `POST /quick-analysis/{id}/images` vom CSRF-Schutz aus. | Auf exakten Pfad umgestellt; ein Test verlangt, dass Ausnahmen nur unauthentifizierte Einstiegspunkte betreffen. | mittel | erledigt |
| 37 | B | `app/core/middleware.py` (`_detect_locale`) | `Accept-Language` ignorierte die Qualitätswerte — es gewann der erste unterstützte Eintrag. Bei `de;q=0.2, en;q=0.9` kam Deutsch heraus, obwohl der Browser ausdrücklich Englisch bevorzugt. | q-Werte werden ausgewertet (ohne Angabe q=1.0, `q=0` heißt „nicht akzeptiert", bei Gleichstand entscheidet die Reihenfolge), fehlerhafte Werte fallen sauber durch. 16 neue Tests. | mittel | erledigt |
| 38 | B | `app/services/analysis/orchestrator.py` | **Locale ging im Executor verloren.** Jedes Analysemodul läuft per `run_in_executor` in einem Worker-Thread — der startet mit leeren `contextvars`, die anfragebezogene Sprache fiel stumm auf Deutsch zurück. Heute folgenlos, weil noch kein Modul `t()` nutzt; genau deshalb eine Falle für den ersten, der es tut (E5). | `contextvars.copy_context()` beim Dispatch; Test prüft auch parallel laufende Module. | mittel | erledigt |
| 39 | B/D | `app/services/visual/cache.py` | Das Cache-Verzeichnis war der **relative** Pfad `backend/uploads/cache` und hing am Arbeitsverzeichnis. Beim dokumentierten Start (`cd backend && uvicorn …`) entstand `backend/backend/uploads/cache` — dieses verwaiste Verzeichnis lag mit **46 Cache-Dateien** im Repo, während das konfigurierte Upload-Volume leer blieb. Im Container heißt das: Cache außerhalb des persistenten Volumes, nach jedem Neustart weg. | Leitet sich aus `settings.UPLOAD_DIR` ab und wird bei relativem Wert auf das Paketverzeichnis bezogen — unabhängig vom Arbeitsverzeichnis. | mittel | erledigt |
| 40 | B | `markdown_knowledge_loader.py::_ensure_loaded` | Lazy-Singleton **ohne Lock**: Zwei gleichzeitig einsteigende Threads parsen den kompletten Korpus doppelt (je ~10 s, ~180 MB). Im Serverbetrieb durch den Startup-Warmup verdeckt, bei Skripten und Workern nicht. | Lock mit zweiter Prüfung im kritischen Abschnitt. | niedrig | erledigt |
| 41 | B | `app/api/routes/knowledge.py:886` | Nacktes `except Exception: pass` im Suchzweig — eine dauerhaft kaputte Korpus-Suche erschien als „keine Treffer" und tauchte in keinem Log auf. | Wird geloggt; die übrigen Treffer werden weiterhin geliefert. | niedrig | erledigt |
| 42 | B/C | `config.py`, `images.py`, `analyzer.py`, `database.py`, `requirements.txt`, `CLAUDE.md` | **Drei Einstellungen waren deklariert, wurden aber nirgends gelesen** (`MAX_IMAGE_SIZE_MB`, `VISUAL_ANALYSIS_TIMEOUT_SEC`, `DATABASE_POOL_SIZE`) — wer sie setzte, änderte nichts. Besonders heikel, seit sie in `.env.example` dokumentiert sind. Zusätzlich war `shapely` gepinnt, aber in `app/` und `tests/` **nirgends importiert** — es zog die native GEOS-Bibliothek in jedes Image. | Alle drei verdrahtet (Upload-Limit, Vision-Timeout, Pool-Größe — Letztere nur für Server-Datenbanken, SQLite lehnt das Argument ab). `shapely` entfernt und in CLAUDE.md als bewusst nicht enthalten vermerkt. | mittel | erledigt |

| 43 | B/D | `config.py`, `analyzer.py`, `.env.example`, `CLAUDE.md` | **Pipeline B war unbenutzbar: das konfigurierte Modell existiert nicht mehr.** `ANTHROPIC_MODEL=claude-sonnet-4-20250514` (und `MODEL_DETAILED=claude-opus-4-20250514`) sind zurückgezogen; die API antwortete mit `404 not_found_error`, die visuelle Analyse schlug bei **jedem** Bild fehl. Beim ersten echten Aufruf dieses Durchlaufs aufgedeckt — vorher war Pipeline B nur gemockt geprüft. | Auf `claude-opus-5` umgestellt (aktuelle IDs tragen kein Datumssuffix), an allen vier Stellen inkl. Spezifikation. | kritisch | erledigt |
| 44 | B | `app/services/visual/analyzer.py` | Ein **404** wurde als „vorübergehender" Fehler eingestuft und viermal mit Backoff wiederholt — 7 Sekunden und 4 API-Anfragen je Bild, jedes Mal vergeblich. In den Logs sah eine Fehlkonfiguration dadurch wie eine Störung aus. | 404/`not_found` ist jetzt nicht wiederholbar, mit Hinweis auf `ANTHROPIC_MODEL` in der Meldung. | mittel | erledigt |
| 45 | B | `config.py`, `.env.example` | Nachdem `VISUAL_ANALYSIS_TIMEOUT_SEC` verdrahtet war (Befund 42), wirkte der Wert **30 s** erstmals — und war zu knapp: Gemessene Rohlatenz auf `claude-opus-5` sind **56,9 s** je Bild (4.970 Ein-/3.363 Ausgabe-Tokens). Jeder Aufruf lief in den Timeout. | Auf 120 s angehoben, mit der Messung als Begründung dokumentiert. | hoch | erledigt |
| 46 | D | Pipeline B, echter Durchlauf | **Erstmals mit echtem API-Aufruf verifiziert** (vorher durchgängig gemockt). Ergebnis am realen Yacht-Salon-Foto: Score 76,0, Konfidenz `visual_medium` (nicht `visual_high` — Inhaltsrelevanz 0,4, weil nur ein Ausschnitt sichtbar), valides JSON, 8 fachlich korrekte Befunde (u.a. fehlende Schlingerleisten am Tisch) und **7 ausdrückliche „nicht beurteilbar"-Einträge** für alles außerhalb des Bildausschnitts. Der Konfidenz-Wächter stuft also korrekt ab, statt Vollständigkeit vorzutäuschen. | Kein Fix nötig — Nachweis, dass die Kernzusage des Systems im Echtbetrieb trägt. | — | verifiziert |

| 47 | B/D | `orchestrator.py::_compute_overall_score` (**E2**) | **Ein dokumentierter Schaden hob die Gesamtnote.** `service_patterns` war ein gewichtetes Mittelwert-Mitglied und zog den Schnitt nach oben, sobald es darüber lag (gemessen 63,0 → 64,0). Der Ursprung war in den Gewichten sichtbar: `OVERALL_WEIGHTS` summierte in **allen** Klassen auf **1,05** — die überzähligen 0,05 waren exakt das nachträglich angehängte service_patterns-Gewicht. | Das Modul geht jetzt als **Abzug** ein (`PENALTY_MODULES`): 100 Punkte kosten nichts, jeder Punkt darunter zieht anteilig ab. Damit ist das Vorzeichen garantiert; gemessen fällt die Note jetzt monoton (61,8 → 61,5 → 61,2 → 60,1). Die Entwurfsgewichte summieren wieder exakt auf 1,0. Die **Höhe** bleibt über `OVERALL_WEIGHTS` steuerbar — sie ist Produktkalibrierung, das Vorzeichen war ein Fehler. 12 neue Tests; 3 Orchestrator-Tests auf den neuen Vertrag gezogen. | hoch | erledigt |
| 48 | B | `ergonomics.py`, `volume_storage.py`, `emotional.py` (**E5-Vorarbeit**) | **47 Warnungen ohne stabilen `code`** (15/15/17). Ohne Code lässt sich eine Warnung weder übersetzen noch auf einen Hilfeartikel verlinken — es ist die Vorbedingung dafür, die Übersetzung aus den Analysemodulen in die Präsentationsschicht zu heben (E5, Option A). | Alle 47 mit sprechenden, stabilen Codes versehen (`ERGO_*`, `VOL_*`, `EMO_*`). Drei neue Tests erzwingen: jede Warnung hat einen Code, Codes sind einheitlich geformt, ein Code steht nie für zwei Schweregrade. | mittel | erledigt |
| 49 | D | `visual_fusion.py` (neu), `orchestrator.py`, `layouts.py` (**E3**) | **Fotos beeinflussten keinen einzigen Score.** `score_fusion.py` war implementiert und getestet, hatte aber **keinen Aufrufer** — die in CLAUDE.md dokumentierte Gewichtstabelle steuerte nichts. Zwei Dinge fehlten dazwischen: die Fusion erwartet Ergebnisse je **Modul**, der Analyzer liefert sie je **Bild**; und sie erwartet `confidence` als **String**, der Analyzer liefert ein **Dict** — `CONFIDENCE_DISCOUNT.get(dict)` wäre mit `TypeError: unhashable type: 'dict'` abgestürzt. Die Fusion war also nicht nur unverdrahtet, sie wäre beim bloßen Anschließen gescheitert. | Neues Bindeglied `visual_fusion.py` (Zuordnung Bild→Modul aus dem **Prompt** abgeleitet, Konfidenz-Normalisierung, unbrauchbare Befunde werden verworfen), Verdrahtung im Orchestrator vor der Notenbildung, Laden der gespeicherten Bildanalysen in der Route. Gemessen: ohne Foto 61,8 → mit gutem Foto 65,4 (6 Module fusioniert); unbrauchbares Foto verhält sich wie kein Foto. **Widerspruchsregel verifiziert**: bei 47 Punkten Abstand bleibt die strukturierte Note stehen, `needs_review: True`, Gewichte 1,0/0,0 — geflaggt statt gemittelt. 21 neue Tests; die beiden Spezifikations-Wächter, die den unverdrahteten Zustand festhielten, sind umgedreht statt gelöscht. | hoch | erledigt |
| 50 | A | 12 Korpusdokumente (**E4**) | Sechs Themen-Dubletten, unabhängig geschrieben (~2 % gemeinsame Zeilen). Ein Widerspruch war bereits belegt: die DIN-766-Teilung. Web-verifiziert — **8 mm → 24 mm, 10 mm → 28 mm**; damit stimmt `13_02`, und die Teilungsspalte in `17_02` ist falsch (ihr 8-mm-Wert ist die 10-mm-Teilung, die Spalte ist verschoben). Folge im Ernstfall: Die Kette passt nicht auf die Kettennuss und springt beim Ankermanöver. | Widerspruch in `17_02` mit Quellen geflaggt und auf die maßgebliche Tabelle verwiesen. Alle 12 Dokumente gegenseitig verlinkt, mit Hinweis auf das Driftrisiko. **Bewusst nicht zusammengeführt** — siehe E4 unten. | mittel | teilweise |
| 51 | D | `analyzer.py`, `images.py` (Latenz) | Meine frühere Vermutung, `effort` sei der Hebel gegen die 61 s, war **falsch** — gemessen: low 52,0 s / medium 61,5 s / high 58,2 s, identischer Score. Die Zeit ist **ausgabetoken-gebunden** (~60 Tokens/s bei ~3.400 Tokens), nicht denkzeit-gebunden. Fast Mode (der richtige Hebel) ist auf diesem Konto rate-limitiert und blieb ungemessen. **Der eigentliche Befund liegt woanders:** Der 60-s-Aufruf läuft **inline in der HTTP-Anfrage** (kein `BackgroundTasks`) — Browser, nginx und die PaaS-Proxys brechen typischerweise bei 60 s ab. | Messung dokumentiert, Vermutung korrigiert. Die Umstellung auf einen Hintergrundauftrag ist ein Architektureingriff → siehe E6. | mittel | empfohlen |

| 52 | B/D | `warning_i18n.py` (neu), `ergonomics.py`, `volume_storage.py`, `emotional.py`, `layouts.py` (**E5**) | i18n endete an der Modulgrenze: Der Katalog war vollständig, aber `t()` wurde in **keinem** der zwölf Analysemodule aufgerufen — ein EN-Lauf lieferte 237 deutsche Strings. | **Option A umgesetzt** (Übersetzung an der Präsentationsgrenze, Module bleiben reine Funktionen): Warnungen tragen `code` + `params`, `localize_warning` setzt die Meldung in der aktiven Sprache zusammen. Verdrahtet in der Vollanalyse-Route. Gespeichert wird **sprachneutral** — dieselbe Analyse ist später in einer anderen Sprache darstellbar. Fehlt eine Übersetzung, gewinnt der deutsche Originaltext. Auch Zonentyp-Bezeichnungen werden übersetzt (sonst stünde „Critical zone missing: **Maschinenraum**"), Nutzernamen dagegen nicht. 20 Codes × 4 Sprachen; im Realllauf **13 von 39** Warnungen übersetzt (33 %), Rest deutsch. 18 neue Tests. | hoch | teilweise |
| 53 | A | 6 Dokumentpaare (**E4**) | Versuch, Widersprüche zwischen den Dubletten **automatisch** zu finden: Abgleich der Zahlen je gemeinsamem Tabellen-Zeilenschlüssel. | **Ergebnis: untauglich.** 27 Kandidaten, die geprüften durchweg Fehlalarme — derselbe Zeilenschlüssel steht in beiden Dokumenten in verschiedenen Tabellenkontexten. Beispiel `Ruderkraft`: in `14_04` die **Balance in Prozent** (15–25 %), in `20_03` die **resultierende Kraft in Newton** (500–5.000 N). Kein Widerspruch, nur gleiche Beschriftung. Der eine echte Widerspruch (DIN-766-Teilung) war durch **Lesen** vergleichbarer Tabellen gefunden worden, nicht durch Mustersuche. Festgehalten, damit niemand denselben Weg nochmal geht. | mittel | geprüft, verworfen |

---

## Empfehlungen mit Tragweite (bewusst NICHT allein entschieden)

### E1 — ERLEDIGT: Der Anthropic-API-Key ist rotiert
Der alte Schlüssel wurde widerrufen, ein neuer erstellt und in `backend/.env` hinterlegt (gitignoriert, nie getrackt). `.env.local` ist aus dem Tracking entfernt und lokal gelöscht. Der Eintrag in Commit `d5162c5` bleibt in der History, ist aber wertlos.

<details><summary>Ursprüngliche Handlungsanweisung</summary>

Der Schlüssel ist aus dem Tracking entfernt, steht aber weiterhin in der Git-History (Commit `d5162c5`).
Entfernen aus HEAD ≠ entfernen aus der History.
- **Option A (empfohlen):** Schlüssel in der Anthropic-Konsole widerrufen und neu ausstellen. Sofort wirksam, kein History-Eingriff.
- **Option B (zusätzlich):** History mit `git filter-repo` bereinigen. Schreibt alle Commit-Hashes um — nur sinnvoll, solange das Repo nicht geteilt ist.
- Ohne A bleibt der Schlüssel kompromittiert, egal was mit der History passiert.
</details>

### E2 — ERLEDIGT (Befund 47): Darf ein dokumentierter Schaden die Gesamtnote anheben?
Gemessen nach allen Korrekturen: Zwei Serviceberichte (1× kritisch, 1× hoch) heben die Gesamtnote von **63,0 auf 64,0**.
Ursache ist nicht mehr das Modul (dessen Note fällt jetzt korrekt), sondern die **Zusammensetzung der Gesamtnote**:
`service_patterns` hat Gewicht 0,05 und liegt mit 78,4 über dem gewichteten Mittel von 63,0 — jedes Modul über dem Mittel hebt den Schnitt.
- **Option A:** `service_patterns` als **Abzug** statt als Mittelwert-Mitglied führen (`gesamt = basis − schadenslast`). Fachlich am ehrlichsten, ändert aber die Bedeutung der Gesamtnote.
- **Option B:** Gewicht deutlich anheben (0,05 → 0,15–0,20). Kleiner Eingriff, löst das Vorzeichenproblem aber nicht grundsätzlich.
- **Option C:** Schadenshistorie gar nicht in die eine Zahl falten, sondern separat ausweisen (für Marc/Kai vermutlich am verständlichsten).
- Nebenbefund: `OVERALL_WEIGHTS["cruising_sail"]` summiert auf **1,05** statt 1,0 (harmlos, da über die beitragenden Gewichte normiert wird — aber es kaschiert Tippfehler).

### E3 — ERLEDIGT (Befund 49): Score-Fusion ist jetzt verdrahtet
`score_fusion.py` hat **keinen einzigen Produktionsaufrufer**: Fotos beeinflussen derzeit keinen Score.
CLAUDE.md ist entsprechend korrigiert (als nicht verdrahtet gekennzeichnet), der Zustand bleibt aber eine Produktlücke.
Erschwerend: Das Modul würde mit dem heutigen Ergebnisformat des Analyzers abstürzen (Befund B-17) — es ist also nicht „nur anschließen".
- **Option A:** Fusion im Orchestrator verdrahten (Tier 2 nach der visuellen Analyse), Formatadapter bauen, Discrepancy-Regel testen. Mehrtägige Arbeit, macht die dokumentierten Fusionsgewichte erst real.
- **Option B:** Visuelle Befunde bewusst getrennt ausweisen und die Fusionsgewichte aus der Spezifikation streichen.
- Nicht empfohlen: den jetzigen Zustand belassen, ohne ihn zu benennen — dann steht in der Spezifikation eine Tabelle, die nichts steuert.

### E5 — MECHANISMUS FERTIG (Befunde 48, 52): i18n endet an der Modulgrenze
`t()` wird nur in `middleware.py`, `permissions.py` und `subscription.py` aufgerufen. **Kein einziges der 12 Analysemodule benutzt i18n.**
Ein Vollanalyse-Lauf mit `set_locale("en")` liefert 237 deutsche Strings im JSON — 97 aus hartkodierten Modul-Literalen, 140 aus dem deutschsprachigen Wissenskorpus.
Der Katalog selbst ist vollständig (232 Schlüssel, 0 Lücken in EN/ES/FR); die technischen Defekte sind behoben (Zahlformat, 5 fehlende Bootsklassen). Das Problem ist die **Reichweite**, nicht der Mechanismus.
- **Option A (architektonisch sauber):** Analysemodule geben nur noch `code` + strukturierte Parameter aus, die Übersetzung passiert in der Präsentationsschicht. Reine Funktionen sollten ohnehin keine lokalisierte Prosa erzeugen. Vorarbeit ist geleistet — jede Warnung hat jetzt `severity` und `suggestion`; es fehlen noch `code` an 47 Stellen in ergonomics/volume_storage/emotional.
- **Option B (pragmatisch):** Modul-Strings über `t()` mit Schlüsseln versehen (~97 Strings × 4 Sprachen). Mechanisch, aber die Interpolation von Messwerten macht viele Schlüssel unhandlich.
- **Option C (ehrlich):** DE als einzige Ergebnissprache dokumentieren und `Feature.MULTI_LANGUAGE` streichen, bis A oder B umgesetzt ist.
- Der Korpusanteil (140 Strings) ist in keiner Variante kurzfristig lösbar — 850.000 Zeilen Fachtext sind nicht nebenbei übersetzbar. Das gehört ausdrücklich benannt, statt Mehrsprachigkeit zu versprechen.


**Stand nach Befund 52:** Der Mechanismus steht und ist verdrahtet; 20 Codes sind in allen vier Sprachen hinterlegt, im Realllauf sind 13 von 39 Warnungen übersetzt (33 %).
**Was bleibt:** Die übrigen 135 Codes. Das ist reine Fleißarbeit nach feststehendem Muster (Template + `params` am Fundort) und braucht keine Architekturentscheidung mehr — aber es sind 135 × 4 fachlich korrekte Übersetzungen.
**Nicht lösbar bleibt der Korpusanteil:** 140 der 237 deutschen Strings stammen aus dem Wissenskorpus. 850.000 Zeilen Fachtext sind nicht nebenbei übersetzbar; das gehört benannt, statt Mehrsprachigkeit zu versprechen.

### E4 — SO WEIT WIE VERANTWORTBAR (Befunde 50, 53): Themen-Dubletten im Korpus
13_02/17_02 Ankerketten, 13_03/17_03 Ankerwinden, 13_04/17_04 Ankergeschirr, 14_03/20_02 Hydraulische Steuerung, 14_04/20_03 Ruderanlage, 14_07/20_04 Steuerräder.
Es sind **unabhängig geschriebene** Texte (je ~3.800 Zeilen, nur ~2 % gemeinsame Zeilen) — sie werden auseinanderdriften und tun es bereits (belegt: widersprüchliche DIN-766-Kettenteilung, beide als Fakt ausgeliefert).
- **Option A:** Zusammenführen, eine Kategorie behält das Dokument, die andere bekommt einen Verweis. Inhaltliche Arbeit, dauerhaft die sauberste Lösung.
- **Option B:** Beide behalten, aber gegeneinander abgleichen und gegenseitig verlinken.
- Der Loader behandelt die Kollisionen bereits verlustfrei (Komposit-Schlüssel) — das Problem ist redaktionell, nicht technisch.

---


**Stand nach Befunden 50 und 53:** Alle 12 Dokumente sind gegenseitig verlinkt und tragen einen Hinweis auf das Driftrisiko. Der eine belegte Widerspruch (DIN-766-Teilung) ist web-verifiziert, geflaggt und auf die maßgebliche Tabelle verwiesen.
**Warum hier Schluss ist:** Ein automatischer Widerspruchsabgleich ist an diesen Dokumenten nachweislich untauglich (Befund 53) — gleiche Zeilenschlüssel, verschiedene Größen. Und ein echtes Zusammenführen bedeutet, ~46.000 Zeilen Fachtext redaktionell zu verschmelzen und dabei zu entscheiden, welche Fassung gilt. Das ist Facharbeit mit realem Risiko, Inhalte zu verlieren — kein Refactoring. Ich halte es für falsch, das nebenbei zu erledigen.
- **Option A:** Ein Paar exemplarisch zusammenführen (Ankerketten, wo der Widerspruch belegt ist), Aufwand messen, dann über die übrigen fünf entscheiden.
- **Option B:** Bei Verlinkung belassen. Die Dokumente ergänzen einander inhaltlich; das Risiko ist Drift, nicht Fehlinformation.

### E6 — Die Bildanalyse blockiert die HTTP-Anfrage (NEU)
Gemessen: **56–62 s** je Bild, und der Aufruf läuft **inline im Request-Handler** (`images.py`, kein `BackgroundTasks`).
Browser, nginx (Default 60 s) und die PaaS-Proxys brechen in dieser Größenordnung ab — der Pfad wird in Produktion sporadisch scheitern, obwohl er lokal funktioniert.
Die Denktiefe ist **nicht** der Hebel (gemessen: low 52,0 / medium 61,5 / high 58,2 s bei identischem Score); die Zeit ist ausgabetoken-gebunden.
- **Option A (empfohlen):** Upload antwortet sofort mit `202` und einer Analyse-ID, die Auswertung läuft als Hintergrundauftrag, das Frontend fragt den Status ab. Löst das Problem unabhängig von Modell und Latenz.
- **Option B:** Fast Mode (`speed: "fast"`, nur Opus 5/4.8) — laut Dokumentation bis 2,5× höhere Ausgaberate bei Aufpreis. Auf diesem Konto rate-limitiert, **ungemessen**.
- **Option C:** Kompakteres Ausgabeschema (weniger zu erzeugende Tokens). Senkt die Analysetiefe.
- Nicht empfohlen: so lassen. Der Pfad funktioniert lokal und fällt erst hinter einem Proxy um.

---

## Schlussbilanz — was jetzt einsatzfähig ist

### Bereich A — Recherche-Wissensbasis
**Abdeckungsgrad (gemessen, nicht geschätzt):** 261 nummerierte Dateien auf der Platte, davon **260 kanonisch geladen** (die 261. ist eine bewusst übersprungene ältere Zwischenkopie) in 32 Kategorien, 847.782 Zeilen.
Nach Strukturprofil: **179/260 mit vollem Profil** (≥1.500 Zeilen und ≥3 strukturierte Bestandteile), **68 teilweise**, **13 dünn** — die 13 sind im Wesentlichen Kategorie 32 (Designsprachen), inhaltlich ein anderes Genre als eine Materialreferenz und dort fair bewertet.

**Der größte Hebel war nicht der Inhalt, sondern die Lesbarkeit für die Maschine:** Der Korpus ist in mehreren Dokumentkonventionen gewachsen, die Extraktoren kannten nur eine. Vorher lieferten **22/260** Dokumente Herstellerdaten und **8/260** Erfahrungsberichte — das Wissen stand im Text, kam aber weder in einer Analyse noch im Lexikon an. Jetzt: **163/260** (2.422 Herstellereinträge) und **61/260** (1.709 Erfahrungsberichte, mit Quelle, Jahr, Modell- und Revierbezug).

Dasselbe Muster fand sich ein drittes Mal bei den **Fehlerbildern**: nur 322 von 985 hatten eine Maßnahme, 428 bestanden nur aus einem Titel. Nach Vereinheitlichung der Label-Definition: **Maßnahme 516, Symptom 641, „nur Titel" 229**.
Die **Lexikon-Suche** findet jetzt Dokumente über ihren Titel, durchsucht Tabellen und sortiert nach Relevanz — vorher lieferte sie unsortierte Treffer, in denen das passende Dokument oft gar nicht vorkam.
Behoben außerdem: sicherheitskritisch erfundene GMDSS-Produkte, vier ISO-Fehlzitate, widersprüchliche Korpuszahlen.
**Offen:** 6 Themen-Dubletten (E4), Kategorien 27–30 ohne regionale Bezugsquellen/Preise, ein vollständiger Faktencheck aller 260 Dokumente (nur stichprobenartig geprüft).

### Bereich B — Code & Backend
**1.775 Tests grün** (vorher 1.224), Backend startet sauber, Korpus wird beim Start vorgewärmt.
Kaltstart ist jetzt dokumentiert: neues README mit verifiziertem Schnellstart, `.env.example` deckt alle 18 Settings ab (vorher 5, ohne die sicherheitsrelevanten).
Zwei Vokabulare, die still auseinandergelaufen waren, werden jetzt aus ihrer Einzelquelle abgeleitet — Zonentypen und Bootsklassen; bei letzteren wurden **5 der 13 offiziellen Klassen** von der Validierung abgelehnt.
Das Rate-Limit trifft jetzt die Routen, für die es gedacht war: Bild-Upload und CAD-Import lagen bei 120/min statt 10/min — ausgerechnet die Pfade, die kostenpflichtige Vision-Aufrufe bzw. CPU-intensives Parsen auslösen.
Drei Einstellungen, die nichts bewirkten (`MAX_IMAGE_SIZE_MB`, `VISUAL_ANALYSIS_TIMEOUT_SEC`, `DATABASE_POOL_SIZE`), sind verdrahtet; `shapely` war gepinnt, aber nirgends importiert, und ist entfernt.
Behoben: kritischer Secret-Leak im Tracking, toter Produktions-Sicherheitsguard, fehlende Sicherheitsheader, NaN/Infinity auf allen Eingabeebenen, fehlende Upload-Limits, Prompt-Injection, wirkungsloser Konfidenzwächter, fehlendes Tarif-Gating für kostenpflichtige Vision-Aufrufe.
**Substanziell korrigiert wurden drei Fälle, in denen das System Falsches als gemessen ausgab:** erfundene DXF-Durchgangsbreiten, ein interner Fehler der zur schlechten Bootsnote wurde, und eine Schadenshistorie ohne jede Wirkung der Schwere.
Zusätzlich: **jede** Analysewarnung trägt jetzt einen Handlungsvorschlag (vorher 17 von 90 ohne) — wo der Korpus keine Maßnahme führt, verweist sie auf den Quellartikel, statt einen Rat zu erfinden.
**Offen:** i18n endet an der Modulgrenze (Analyseergebnisse sind unabhängig vom Locale deutsch), Frontend hat keinen i18n-Mechanismus — siehe E5.

### Bereich C — Spezifikation
CLAUDE.md und KNOWLEDGE_INDEX stimmen wieder mit dem Code überein; 21 Tests halten das dauerhaft.
Neu ist ein verifizierter „Getting Started"-Abschnitt — ein neuer Durchlauf ist mit CLAUDE.md allein arbeitsfähig.
Nicht implementierte Teile sind jetzt als solche gekennzeichnet statt als Ist beschrieben.

### Bereich D — Systemintegration
**Alle drei Eingabemodalitäten end-to-end über HTTP nachgewiesen:**
1. **CAD:** DXF parsen (5 Zonen, 4 Durchgänge, Breiten ehrlich `None`) → Layout anlegen → Vollanalyse: 6 Module, Note 63,0 `measured`, 6 mangels Daten mit Klartextgrund übersprungen, 0 Fehler, keine abgestürzte Teilanalyse.
2. **Text:** Servicebericht anlegen → `service_patterns` läuft → wirkt auf die Gesamtnote.
3. **Foto:** PRO löst die Analyse aus und speichert das Ergebnis; FREE wird serverseitig durch das Tarif-Gate gestoppt — vorher schlug dieser Pfad **bei jedem Aufruf** intern fehl und meldete trotzdem Erfolg. **Mit echtem API-Aufruf nachgewiesen** (Befund 46): Score 76,0, Konfidenz korrekt auf `visual_medium` abgestuft, 7 Punkte ausdrücklich als nicht beurteilbar ausgewiesen.

Praxistest: **123/123, 0 Warnungen** (vorher 107/6 und faktisch nicht lauffähig).
**Frontend-Lücken geschlossen:** Serviceberichte und Kostenpositionen sind jetzt erfassbar, Bilder im Projekt hochladbar, und die Oberfläche unterscheidet vier Modul-Zustände (tarifgesperrt / mangels Daten übersprungen / fehlgeschlagen / teilweise berechnet) statt Module kommentarlos verschwinden zu lassen. Dazu drei Aufrufe repariert, die ins Leere liefen (404, 307-Redirect, nie genutzter Refresh-Token) und die HEIC-Diskrepanz, an der der wahrscheinlichste Upload überhaupt scheiterte.
**Offen und bewusst nicht allein entschieden:** Score-Fusion ist nicht verdrahtet (E3), Vorzeichenfrage der Gesamtnote (E2).

### Ehrliche Einschränkung
Von 123 bestätigten Audit-Befunden ist der P0/P1-Block behoben und getestet. Ein Teil der mittleren und niedrigen Befunde ist dokumentiert, aber nicht umgesetzt — vor allem die Frontend-Lücken und die i18n-Reichweite. Für einen ersten echten Nutzer ist der Backend-Pfad tragfähig; die Oberfläche deckt heute nur einen Teil davon ab.
